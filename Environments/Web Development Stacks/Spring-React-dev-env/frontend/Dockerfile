# Use a recent Node.js LTS version
FROM node:20

# Set the working directory
WORKDIR /app

# Expose the default React port
EXPOSE 3000

# This command starts the React development server
# It will work once the developer creates a React app in the mounted volume
CMD ["npm", "start"]